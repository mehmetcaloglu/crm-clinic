Follow **`AGENTS.md`** for all agent behavior, documentation updates, and project constraints.

Do not duplicate long rules here — read `AGENTS.md`, `.cursor/rules/*.mdc`, and relevant `docs/` before planning or coding.
