# Frontend Screen Plan

Use the **frontend-builder** skill. Read `docs/09-ux-flows.md` and `.cursor/rules/50-frontend-ux.mdc`. **Plan only unless I say implement.**

Screen:

```
[PASTE SCREEN NAME — e.g. Contact detail, Dashboard, Follow-ups]
```

Output: route/path, user goals, component tree, domain service dependencies, loading/empty/error states, files to create/change, manual QA checklist.

Rules: simple UX, no business logic in components, mobile-friendly, no provider payload types in UI.

If I approve, implement in small steps and update docs after.
