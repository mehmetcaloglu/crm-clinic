# Implement Manual CRM (Phase 1)

Implement **one small Phase 1 task** from `docs/tasks/PHASE-1-manual-crm.md`. Follow `.cursor/rules/` and `AGENTS.md`.

Task:

```
[PASTE SINGLE CHECKLIST ITEM]
```

Steps:
1. Read relevant docs and existing code.
2. Short plan + files to change (skip if already approved).
3. Implement only this task — no WhatsApp/Instagram API, no unrelated features.
4. Business logic in `lib/domain/`, not UI components.
5. Add loading/empty/error states for UI work.
6. Run lint/typecheck/build if applicable.
7. Use **process-updater** to update `docs/WORKLOG.md`, `docs/TASKS.md`, and phase file.

Do not install packages unless I explicitly allow it.
